import React from 'react'

function SearchDropdown(props) {
  // if(props.focused) {
  if(true) {
    return (
      <div style={{position:'absolute', width: 'inherit', textAlign: 'left', backgroundColor: '#292929'}}>
        works
      </div>
    )
  } else {
    return (
      <div></div>
    )
  }
}

export default SearchDropdown